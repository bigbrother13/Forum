
<% @topics.each do |topic| %>
  <h2><%= link_to topic.title, topic %></h2>
  <p>
    Published at
    <%= time_ago_in_words(topic.created_at)%>
    <%= link_to topic.user.email, current_user %>

  </p>
<% end %>