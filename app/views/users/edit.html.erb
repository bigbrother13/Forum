<h1>Edit Profile</h1>

<%= simple_form_for(@user, html: { multipart: true }) do |f| %>
  <%= f.label :avatar %>
  <%= f.file_field :avatar%>
  <%= f.input :email %>
  <%= f.submit 'Save changes' %>
<% end %>